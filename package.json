{
    "name": "my-express-app",
    "version": "1.0.0",
    "scripts": {
      "start": "node server.js",
      "build": "rm -rf dist && mkdir dist && npm run build:app",
      "build:app": "cp -R public dist/"
    },
    "dependencies": {
      "express": "^4.17.1"
    }
  }
  