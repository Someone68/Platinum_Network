{
    "name": "my-express-app",
    "version": "1.0.0",
    "scripts": {
      "start": "node server.js",
      "build": "rm -rf dist && mkdir dist && npm run build:css && npm run build:js",
      "build:css": "node-sass public/styles/styles.scss dist/styles.css",
      "build:js": "browserify public/scripts/app.js -o dist/bundle.js"
    },
    "dependencies": {
      "express": "^4.17.1"
    }
  }
  