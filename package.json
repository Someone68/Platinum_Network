{
    "name": "my-express-app",
    "version": "1.0.0",
    "scripts": {
      "start": "node server.js",
      "build": "rm -rf dist && mkdir dist && npm run build:app",
      "build:app": "npx babel public --out-dir dist --copy-files --ignore '**/*.test.js'"
    },
    "dependencies": {
      "express": "^4.17.1"
    },
    "devDependencies": {
      "@babel/cli": "^7.15.0",
      "@babel/core": "^7.15.0",
      "@babel/preset-env": "^7.15.0"
    }
  }
  